- en: TLS auth
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: TLS认证
- en: 'TLS connections offer a (rarely used) feature called Secure Remote Passwords.
    Using this, you authenticate the connection for the server using a name and password
    and the command line flags for this are `--tlsuser <name>` and `--tlspassword
    <secret>`. Like this:'
  id: totrans-1
  prefs: []
  type: TYPE_NORMAL
  zh: TLS连接提供了一种（很少使用）称为安全远程密码的功能。使用它，您可以使用用户名和密码以及命令行标志来认证服务器的连接，这些标志是`--tlsuser
    <name>`和`--tlspassword <secret>`。就像这样：
- en: '[PRE0]'
  id: totrans-2
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
