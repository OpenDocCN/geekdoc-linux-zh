- en: Posting binary
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 发布二进制
- en: 'When reading data to post from a file, `-d` strips out carriage returns and
    newlines. Use `--data-binary` if you want curl to read and use the given file
    in binary exactly as given:'
  id: totrans-1
  prefs: []
  type: TYPE_NORMAL
  zh: 当从文件读取数据以发布时，`-d` 会去除回车和换行符。如果你想让 curl 读取并使用给定的文件作为二进制文件，则使用 `--data-binary`：
- en: '[PRE0]'
  id: totrans-2
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
