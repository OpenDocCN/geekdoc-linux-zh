- en: TFTP
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: TFTP
- en: Trivial File Transfer Protocol (TFTP) is a simple clear-text protocol that allows
    a client to get a file from or put a file onto a remote host.
  id: totrans-1
  prefs: []
  type: TYPE_NORMAL
  zh: 简单文件传输协议（TFTP）是一种简单的明文协议，允许客户端从远程主机获取文件或将文件放置在远程主机上。
- en: Primary use cases for this protocol have been to get the boot image over a local
    network. TFTP also stands out a little next to many other protocols by the fact
    that it is done over UDP as opposed to TCP which most other protocols use.
  id: totrans-2
  prefs: []
  type: TYPE_NORMAL
  zh: 此协议的主要用例是通过本地网络获取引导映像。TFTP与其他许多协议相比，还有一个特点，那就是它使用UDP而不是TCP（大多数其他协议使用TCP）。
- en: There is no secure version or flavor of TFTP.
  id: totrans-3
  prefs: []
  type: TYPE_NORMAL
  zh: 没有TFTP的安全版本或变种。
- en: Download
  id: totrans-4
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 下载
- en: 'Download a file from the TFTP server of choice:'
  id: totrans-5
  prefs: []
  type: TYPE_NORMAL
  zh: 从选择的TFTP服务器下载文件：
- en: '[PRE0]'
  id: totrans-6
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
- en: Upload
  id: totrans-7
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 上传
- en: 'Upload a file to the TFTP server of choice:'
  id: totrans-8
  prefs: []
  type: TYPE_NORMAL
  zh: 将文件上传到选择的TFTP服务器：
- en: '[PRE1]'
  id: totrans-9
  prefs: []
  type: TYPE_PRE
  zh: '[PRE1]'
- en: TFTP options
  id: totrans-10
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: TFTP选项
- en: The TFTP protocols transmits data to the other end of the communication using
    “blocks”. When a TFTP transfer is setup, both parties either agree on using the
    default block size of 512 bytes or negotiate a different one. curl supports block
    sizes between 8 to 65464 bytes.
  id: totrans-11
  prefs: []
  type: TYPE_NORMAL
  zh: TFTP协议通过使用“块”将数据传输到通信的另一端。当设置TFTP传输时，双方要么同意使用默认的512字节块大小，要么协商使用不同的块大小。curl支持8到65464字节的块大小。
- en: 'You ask curl to use a different size than the default with `--tftp-blksize`.
    Ask for 8192 bytes blocks like this:'
  id: totrans-12
  prefs: []
  type: TYPE_NORMAL
  zh: 您可以使用`--tftp-blksize`让curl使用与默认值不同的块大小。例如，请求8192字节的块大小如下：
- en: '[PRE2]'
  id: totrans-13
  prefs: []
  type: TYPE_PRE
  zh: '[PRE2]'
- en: 'It has been shown that there are server implementations that do not handle
    option negotiation at all, so curl also has the ability to completely switch off
    all attempts of setting options. If you are in the unfortunate of working with
    such a server, use the flag like this:'
  id: totrans-14
  prefs: []
  type: TYPE_NORMAL
  zh: 已有证据表明，有些服务器实现根本不处理选项协商，因此curl也有完全关闭设置选项尝试的能力。如果您不幸需要与这样的服务器一起工作，可以使用如下标志：
- en: '[PRE3]'
  id: totrans-15
  prefs: []
  type: TYPE_PRE
  zh: '[PRE3]'
