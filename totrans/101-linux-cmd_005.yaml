- en: The `tail` command
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
- en: The `tail` command prints the last ten lines of a file.
  prefs: []
  type: TYPE_NORMAL
- en: 'Example:'
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE0]'
  prefs: []
  type: TYPE_PRE
- en: 'Syntax:'
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE1]'
  prefs: []
  type: TYPE_PRE
- en: Common Use Cases
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
- en: 'Get a specific number of lines:'
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: Use the `-n` option with a number (should be an integer) of lines to display.
  prefs: []
  type: TYPE_NORMAL
- en: 'Example:'
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE2]'
  prefs: []
  type: TYPE_PRE
- en: This command will display the last ten lines of the file `foo.txt`.
  prefs: []
  type: TYPE_NORMAL
- en: Monitor new entry on files in real-time
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: It is possible to let tail output any new line added to the file you are looking
    into. So, if a new line is written to the file, it will immediately be shown in
    your output. This can be done using the `--follow` or `-f` option. This is especially
    useful for monitoring log files.
  prefs: []
  type: TYPE_NORMAL
- en: 'Example:'
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE3]'
  prefs: []
  type: TYPE_PRE
- en: Press `Ctrl+C` to stop following.
  prefs: []
  type: TYPE_NORMAL
- en: Combine options for log monitoring
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: '[PRE4]'
  prefs: []
  type: TYPE_PRE
- en: Follow multiple files simultaneously
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: '[PRE5]'
  prefs: []
  type: TYPE_PRE
- en: Display specific byte ranges
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: '[PRE6]'
  prefs: []
  type: TYPE_PRE
- en: Follow logs even after rotation
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: '[PRE7]'
  prefs: []
  type: TYPE_PRE
- en: The `-F` option is crucial for monitoring logs managed by logrotate.
  prefs: []
  type: TYPE_NORMAL
- en: Skip header lines
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: '[PRE8]'
  prefs: []
  type: TYPE_PRE
- en: Quiet mode for multiple files
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: '[PRE9]'
  prefs: []
  type: TYPE_PRE
- en: Additional Flags and their Functionalities
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: '| **Short Flag** | **Long Flag** | **Description** |'
  prefs: []
  type: TYPE_TB
- en: '| :-- | :-- | :-- |'
  prefs: []
  type: TYPE_TB
- en: '| `-c` | `--bytes=[+]NUM` | Output the last NUM bytes; or use -c +NUM to'
  prefs: []
  type: TYPE_NORMAL
- en: output starting with byte NUM of each file |
  prefs: []
  type: TYPE_NORMAL
- en: '| `-f` | `--follow[={name&#124;descriptor}]` | Output appended data as the
    file grows; an absent option argument means ''descriptor'' |'
  prefs: []
  type: TYPE_TB
- en: '| `-F` |  | Same as --follow=name --retry |'
  prefs: []
  type: TYPE_TB
- en: '| `-n` | `--lines=[+]NUM` | Output the last NUM lines, instead of the last
    10; or use -n +NUM to output starting with line NUM |'
  prefs: []
  type: TYPE_TB
- en: '|  | `--max-unchanged-stats=N` | with --follow=name, reopen a FILE which has
    not changed size after N (default 5) iterations'
  prefs: []
  type: TYPE_NORMAL
- en: to see if it has been unlinked or rename
  prefs: []
  type: TYPE_NORMAL
- en: (this is the usual case of rotated log files);
  prefs: []
  type: TYPE_NORMAL
- en: with inotify, this option is rarely useful |
  prefs: []
  type: TYPE_NORMAL
- en: '|  | `--pid=PID` | with -f, terminate after process ID, PID dies |'
  prefs: []
  type: TYPE_TB
- en: '| `-q` | `--quiet, --silent` | Never output headers giving file names |'
  prefs: []
  type: TYPE_TB
- en: '|  | `--retry` | keep trying to open a file if it is inaccessible |'
  prefs: []
  type: TYPE_TB
- en: '| `-s` | `--sleep-interval=N` | With -f, sleep for approximately N seconds
    (default 1.0) between iterations;'
  prefs: []
  type: TYPE_NORMAL
- en: with inotify and --pid=P, check process P at
  prefs: []
  type: TYPE_NORMAL
- en: least once every N seconds |
  prefs: []
  type: TYPE_NORMAL
- en: '| `-v` | `--verbose` | Always output headers giving file names |'
  prefs: []
  type: TYPE_TB
- en: '| `-z` | `--zero-terminated` | Line delimiter is NUL, not newline |'
  prefs: []
  type: TYPE_TB
- en: '|  | `--help` | Display this help and exit |'
  prefs: []
  type: TYPE_TB
- en: '|  | `--version` | Output version information and exit |'
  prefs: []
  type: TYPE_TB
- en: Tips
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
- en: Use `-F` instead of `-f` for production log monitoring
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: Combine with `grep`, `awk`, or `sed` for filtered monitoring
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: For large files, `tail` is much faster than opening in an editor
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: Use `-n +N` to start from a specific line (useful for skipping headers)
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
