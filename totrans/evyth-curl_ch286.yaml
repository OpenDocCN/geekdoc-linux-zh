- en: Get an FTP directory listing
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
- en: This example just fetches the FTP directory output from the given URL and sends
    it to stdout. The trailing slash in the URL is what makes libcurl treat it as
    a directory.
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE0]'
  prefs: []
  type: TYPE_PRE
