- en: The `rmdir` command
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
- en: The **rmdir** command is used to remove empty directories from the filesystem
    in Linux. The rmdir command removes each and every directory specified in the
    command line only if these directories are empty.
  prefs: []
  type: TYPE_NORMAL
- en: 'Usage and Examples:'
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: remove directory and its ancestors
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: '[PRE0]'
  prefs: []
  type: TYPE_PRE
- en: remove multiple directories
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: '[PRE1]'
  prefs: []
  type: TYPE_PRE
- en: 'Syntax:'
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: '[PRE2]'
  prefs: []
  type: TYPE_PRE
- en: 'Additional Flags and their Functionalities:'
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: '| **Short Flag** | **Long Flag** | **Description** |'
  prefs: []
  type: TYPE_TB
- en: '| :-- | :-- | :-- |'
  prefs: []
  type: TYPE_TB
- en: '| `-` | `--ignore-fail-on-non-empty` | ignore each failure that is solely because
    a directory is non-empty |'
  prefs: []
  type: TYPE_TB
- en: '| `-p` | `--parents` | remove DIRECTORY and its ancestors |'
  prefs: []
  type: TYPE_TB
- en: '| `-d` | `--delimiter=DELIM` | use DELIM instead of TAB for field delimiter
    |'
  prefs: []
  type: TYPE_TB
- en: '| `-v` | `--verbose` | output a diagnostic for every directory processed |'
  prefs: []
  type: TYPE_TB
