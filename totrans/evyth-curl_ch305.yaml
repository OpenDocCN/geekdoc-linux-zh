- en: Build tests
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
  zh: 构建测试
- en: Before you can run any tests you need to build curl but also build the test
    suite and its associated tools and servers.
  id: totrans-1
  prefs: []
  type: TYPE_NORMAL
  zh: 在你能够运行任何测试之前，你需要构建 curl，同时还需要构建测试套件及其相关的工具和服务器。
- en: 'Most conveniently, you can just build and run them all by issuing `make test`
    in the build directory root but if you want to work more on tests or perhaps even
    debug one, you may want to jump into the `tests` directory and work from within
    that. Build it all and run test `144` like this:'
  id: totrans-2
  prefs: []
  type: TYPE_NORMAL
  zh: 最方便的方法是，你只需在构建目录的根目录下运行 `make test` 就可以构建并运行所有内容，但如果你想更深入地工作于测试或者甚至调试其中一个，你可能需要跳转到
    `tests` 目录并在其中工作。构建所有内容并按照以下方式运行测试 `144`：
- en: '[PRE0]'
  id: totrans-3
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
