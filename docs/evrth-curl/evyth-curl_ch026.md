# 安装 curl 和 libcurl

curl 是完全免费、开源且可用的。对于大多数操作系统和架构，有多种方式可以获取并安装它。本节为您提供了开始的一些答案，但不是完整的参考。

一些操作系统默认安装了 curl，而另一些则没有。

此外，您始终可以从[curl.se](https://curl.se)下载源代码，或从那里找到可下载的二进制包。

通常，libcurl 会与 curl 一起安装。

+   Linux

+   Windows

+   macOS

+   容器
