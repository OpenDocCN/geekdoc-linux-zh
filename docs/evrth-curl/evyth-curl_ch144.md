# 方法

在每一个 HTTP 请求中，都有一个方法。有时也被称为动词。最常用的方法有`GET`、`POST`、`HEAD`和`PUT`。

然而，通常你不会在命令行中指定方法，而是具体使用的方法取决于你使用的特定选项。`GET`是默认方法，使用`-d`或`-F`将其变为`POST`，使用`-I`生成`HEAD`，使用`-T`发送`PUT`。

更多关于这个的内容请参阅修改 HTTP 请求部分。
