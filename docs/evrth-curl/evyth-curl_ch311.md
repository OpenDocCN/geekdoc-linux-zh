# 单元测试

**单元测试仅在调试构建上工作。**

单元测试是使用 libcurl 内部函数进行的测试，因此它们不属于任何公共 API、头文件或外部文档的一部分。

如果您要测试的内部函数被定义为`static`，则应将其设置为`UNITTEST` - 这样调试构建将不会为它们使用`static`，从而使它们可以从单元测试中访问。

我们提供了一套方便的函数和宏，用于单元测试，以便快速轻松地编写它们。

所有单元测试程序都保存在`tests/unit`
