# 在文件系统中存储元数据

当使用 curl 保存下载到文件时，`--xattr`选项指示 curl 还将某些文件元数据存储在“扩展文件属性”中。这些扩展属性是存储在文件系统中的标准名称/值对，前提是使用了支持的文件系统和操作系统。

目前，URL 存储在`xdg.origin.url`属性中，对于 HTTP，内容类型存储在`mime_type`属性中。如果文件系统不支持扩展属性而此选项被设置，则会发出警告。
