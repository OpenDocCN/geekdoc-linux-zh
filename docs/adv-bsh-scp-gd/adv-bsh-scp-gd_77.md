# 索引

> [`tldp.org/LDP/abs/html/xrefindex.html`](https://tldp.org/LDP/abs/html/xrefindex.html)

此索引/词汇表/快速参考列出了文本中涵盖的许多重要主题。术语按 *近似* ASCII 排序顺序排列，*必要时修改* 以提高清晰度。

注意，*命令* 在 第四部分 中有索引。

* * *

**^** (撇号)

+   行首，在一个 正则表达式 中

+   **^**

    **^^**

    参数替换中的大写转换

**~** *波浪号*

+   **~** 主目录，对应于`$HOME`

+   **~/** *当前用户的主目录*

+   **~+** *当前工作目录

+   **~-** *上一个*工作目录

**=** *等号* 符号

+   **=** 变量赋值 运算符

+   **=** 字符串比较 运算符

    **==** 字符串比较 运算符

+   **=~** *正则表达式* 匹配 运算符

    *示例脚本*

**<** 左尖括号

+   小于

    字符串比较

    整数比较 在 双括号 内

+   重定向

    **<** `stdin`

    **<<** *这里文档*

    **<<<** *这里字符串*

    **<>** 打开文件 用于 *读写*

**>** 右尖括号

+   大于

    字符串比较

    整数比较，在 *双括号* 内

+   重定向

    **>** 重定向 `stdout` 到一个文件

    **>>** 重定向 `stdout` 到一个文件，但 *追加*

    **i>&j** 重定向 *文件描述符* `i` 到 *文件描述符* `j`

    **>&j** 重定向 `stdout` 到 *文件描述符* `j`

    **>&2** 重定向命令的 `stdout` 到 `stderr`

    **2>&1** 重定向 `stderr` 到 `stdout`

    **&>** 重定向命令的 *both* `stdout` 和 `stderr` 到一个文件

    **:> `file`** 截断文件 到零长度

**|** 管道，用于将一个命令的输出传递给另一个命令或 shell

**||** 逻辑或测试运算符

**-** (破折号)

+   参数替换中的*默认参数*前缀

+   选项标志的前缀

+   指示 *重定向* 从 `stdin` 或 `stdout`

+   **--** (双横线)

    长命令选项的前缀

    双括号内的 *C* 风格变量递减

**;** (分号)

+   作为命令分隔符

+   **\;** 转义的分号，终止 find 命令

+   **;;** 双分号，case 选项中的终止符

    在 ... 时需要

    *do* 关键字位于 *循环* 的第一行

    终止 *花括号* 代码块

+   **;;&** **;&** 终止符 在 *case* 选项中 (Bash 版本 4+)

**:** 冒号

+   **:> `filename`** 截断文件 到零长度

+   *null* 命令，相当于 Bash 内置的 true

+   用于 匿名 here 文档

+   用于 否则为空的函数

+   用作 函数名

**!** 否定运算符，反转测试或命令的 退出状态

+   **!=** 不等于 字符串比较运算符

**?** (问号)

+   匹配零个或一个字符，在 扩展正则表达式 中

+   单字符 *通配符*，在 通配符匹配 中

+   在 *C* 风格的三元运算符 中

**//** 双正斜杠，cd 命令的行为

**.** (点 / 句号)

+   **.** 加载文件（到脚本中），相当于 source 命令

+   **.** 匹配单个字符，在 正则表达式 中

+   **.** 当前工作目录

    **./** 当前工作目录

+   **..** *父目录*

**' ... '** (单引号) *强引用*

**" ... "** (双引号) *弱引用*

+   *双引号* 转义 *反斜杠* （**\**）字符

**,**

+   逗号运算符

+   **,**

    **,,**

    *参数替换* 中的 小写转换

**()** 括号

+   **( ... )** 命令分组；启动 子 shell

+   **( ... )** 扩展正则表达式的 *分组*

+   **>( ... )**

    **<( ... )** 进程替换

+   **... )** 在*case*结构中终止测试条件

+   **(( ... ))** 双括号，在算术扩展中

**** [左括号，*测试*结构

**[ ]** 括号

+   *数组*元素

+   在*正则表达式*中包围要匹配的字符集

+   *测试*结构

**[[ ... ]]** 双中括号，扩展*测试*结构

**$** *锚点*，在正则表达式中

**$** 变量名的**前缀**

**$( ... )** 命令替换，使用括号符号设置变量，以命令的输出

**` ... `** 命令替换，使用反引号符号

**$[ ... ]** 整数扩展（已弃用）

**${ ... }** 变量操作/评估

+   **${var}** 变量的值

+   **${#var}** 变量的长度

+   **${#@}**

    **${#*}** 位置参数的数量

+   **${parameter?err_msg}** 未设置参数的消息

+   **${parameter-default}**

    **${parameter:-default}**

    **${parameter=default}**

    **${parameter:=default}** 设置默认参数

+   **${parameter+alt_value}**

    **${parameter:+alt_value}**

    参数的**备选值**，如果已设置

+   **${!var}**

    变量的**间接引用**，新符号

+   **${!#}**

    最后的**位置参数**。(这是对$#的**间接引用**。)

+   **${!varprefix*}**

    **${!varprefix@}**

    匹配以`varprefix`开头的所有已声明的变量**names**

+   **${string:position}**

    **${string:position:length}** 子串提取

+   **${var#Pattern}**

    **${var##Pattern}** 子串删除

+   **${var%Pattern}**

    **${var%%Pattern}** 子串删除

+   **${string/substring/replacement}**

    **${string//substring/replacement}**

    **${string/#substring/replacement}**

    **${string/%substring/replacement}** 子串替换

**$' ... '** 字符串扩展，使用*转义*字符。

**\** 转义后面的字符

+   **< ... >** 尖括号，*转义*，在正则表达式中的单词边界

+   **\{ N \}** “花括号”，*转义*，在扩展 RE 中匹配字符集的数量

+   **\;** 分号，*转义*，终止一个 find 命令

+   **\$$** 变量的间接引用，旧式表示法

+   转义*换行符*，以写入多行命令

**&**

+   **&>** 将命令的 *both* `stdout` 和 `stderr` 重定向到一个文件

+   **>&j** 将 `stdout` 重定向到 *文件描述符* *j*

    **>&2** 将命令的 `stdout` 重定向到 `stderr`

+   **i>&j** 将 *文件描述符* *i* 重定向到 *文件描述符* *j*

    **2>&1** 将 `stderr` 重定向到 `stdout`

+   关闭*文件描述符*

    **n<&-** 关闭输入文件描述符 *n*

    **0<&-**，**<&-** 关闭 `stdin`

    **n>&-** 关闭输出文件描述符 *n*

    **1>&-**，**>&-** 关闭 `stdout`

+   **&&** 逻辑 AND 测试运算符

+   **命令 &** 在*后台*运行作业

**#** Hashmark，开始脚本的特殊符号

**#!** Sha-bang，以特殊字符串开始的 shell 脚本

***** 星号

+   *通配符*，在 globbing 中

+   正则表达式中的任意数量的字符

+   ****** 指数运算，算术运算符

+   ****** 扩展 *globbing* 文件匹配运算符

**%** 百分号

+   取模，除法余数算术运算

+   子串删除（模式匹配）运算符

**+** 加号

+   *字符匹配*，在扩展正则表达式中

+   前缀到*替代参数*，在*参数替换*中

+   **++** C 风格变量递增，在 双括号 内

* * *

*Shell 变量*

**$_** 上一条命令的最后一个参数

**$-** 传递给脚本的*标志*，使用 set

**$!** 最后后台作业的*进程 ID*

**$?** 命令的*退出状态*

**$@** 所有 *位置参数*，作为*单独*的单词

**$*** 所有 *位置参数*，作为一个*单个*单词

**$$** 脚本进程 ID

**$#** 传递给 [函数 或脚本本身的](internalvariables.html#CLACOUNTREF) 参数数量

**$0** 脚本的文件名

**$1** 传递给脚本的第一个参数

**$9** 传递给脚本的第九个参数

**表格** 的 *shell 变量*

* * * * * *

**-a** 逻辑 AND 复合比较测试

地址数据库，脚本示例

*高级 Bash 脚本指南*，下载位置

别名

+   使用 *unalias* 删除 *别名*

字母表排序

*与* 列表

+   提供默认命令行参数

*逻辑与* 运算符 **&&**

尖括号，*转义*，**< . . . >** 在 正则表达式 中的单词边界

匿名 *here document*，使用 **:**

归档

+   rpm

+   tar

算术扩展

+   *退出状态*

+   变体

算术运算符

+   组合运算符，*C* 风格

    **+=** **-=** ***=** **/=** **%=**

    | ![注意](img/068cd6dc5ba56f1437f9ae6e0cb52ede.png) | 在 某些上下文 中，**+=** 也可以作为 *字符串连接* 运算符。 |
    | --- | --- |

数组

+   关联数组

    比传统数组更高效

+   方括号表示法

+   连接，*示例脚本*

+   复制

+   声明

    `declare -a array_name`

+   嵌入式数组

+   空数组，空元素，*示例脚本*

+   间接引用

+   初始化

    `array=( element1 element2 ... elementN)`

    *示例脚本*

    使用 命令替换

+   将文件加载到数组中

+   多维，模拟

+   嵌套和嵌入

+   表示法和用法

+   数组中的元素数量

    `${#array_name[@]}`

    `${#array_name[*]}`

+   操作

+   将 *数组* 传递给函数

+   作为 函数的 *返回值*

+   特殊属性，示例脚本

+   字符串操作，示例脚本

+   *unset* 删除数组元素

箭头键，检测

ASCII

+   定义

+   生成 ASCII 表的脚本

awk 字段导向的文本处理语言

+   `rand()`，随机函数

+   字符串操作

+   使用 *export* 将变量传递给嵌入的 *awk* 脚本

* * *

背光，设置亮度

反引号，用于 命令替换

基数转换，*示例脚本*

Bash

+   不良的脚本编写习惯

+   基础知识回顾，*脚本示例*

+   命令行选项

    **表格**

+   经典 *Bourne* shell 缺少的特性

+   内部变量

+   版本 2

+   版本 3

+   版本 4

    版本 4.1

    版本 4.2

.bashrc

`$BASH_SUBSHELL`

基本命令，外部

批处理文件, *DOS*

批量处理

bc, 计算器工具

+   在 *here 文档* 中

+   模板 用于计算脚本变量

参考文献

Bison 工具

位运算符

+   示例脚本

块设备

+   测试

代码块

+   迭代/循环

+   重定向

    *脚本示例*：重定向代码块的输出

可启动的闪存驱动器, 创建

花括号展开

+   扩展，`*{a..z}*`

+   参数化

+   增量填充（Bash 的新特性，版本 4）

括号，**[ ]**

+   *数组* 元素

+   匹配字符集 在 *正则表达式* 中括号内

+   *测试* 结构

括号，*花括号*，**{}**，用于

+   代码块

+   *find*

+   *扩展正则表达式*

+   *位置参数*

+   *xargs*

break *循环* 控制命令

+   参数 (可选)

内置 在 *Bash* 中

+   不要在子进程中调用子进程

* * *

*case* 构造

+   命令行参数，处理

+   通配符，使用通配符过滤字符串

cat，连接文件(s)

+   滥用

+   *cat* 脚本

+   比重定向 `stdin` 效率低

+   将输出管道连接到，到一个 读取

+   用法

字符设备

+   测试

校验和

子进程

冒号，**:** ，等同于 true Bash 内置命令

着色脚本

+   在背景颜色之间循环，示例脚本

+   **表格** 着色转义序列

+   模板，着色背景上的着色文本

逗号运算符，连接命令或操作

命令行选项

command_not_found_handle () *内置* 错误处理函数 (Bash 版本 4+)

命令替换

+   **$( ... )**，首选表示法

+   *反引号*

+   扩展 *Bash* 工具集

+   调用一个 *子 shell*

+   嵌套

+   删除尾随换行符

+   从循环输出设置变量

+   单词分割

注释头，特殊用途

注释代码块

+   使用一个 *匿名* 这里文档

+   使用一个 *if-then* 构造

通信和主机

复合比较 运算符

压缩工具

+   bzip2

+   compress

+   gzip

+   zip

continue 循环控制命令

控制字符

+   Control-C，*中断*

+   Control-D，终止 / 登出 / 清除

+   Control-G，`**BEL**` (*蜂鸣*)

+   Control-H，*删除*

+   Control-J，*换行*

+   Control-M，回车

协处理进程

cron，调度 *守护进程*

*C*-style syntax ，用于处理变量

纵横字谜求解器

密码学

大括号 {}

+   在 *find* 命令中

+   在 *扩展正则表达式* 中

+   在 *xargs* 中

* * *

守护进程，在 UNIX 类型操作系统

date

dc，计算器实用程序

dd，*数据复制器* 命令

+   转换

+   复制原始数据 到/从设备

+   文件删除，*安全*

+   按键，捕获

+   选项

+   在数据流上的随机访问

+   *树莓派*，准备可启动 SD 卡的脚本

+   交换文件，初始化

+   在 *www.linuxquestions.org* 上的线程

调试脚本

+   工具

+   *退出时捕获*

+   *捕获* 信号

十进制数，Bash 将数字解释为

declare 内置命令

+   选项

    case-modification 选项（Bash 版本 4+）

默认参数

`/dev` 目录

+   `/dev/null` 伪设备文件

+   `/dev/urandom` 伪设备文件，生成伪随机数

+   `/dev/zero`，伪设备文件

设备文件

*dialog*，用于在脚本中生成 *对话框* 的实用程序

`$DIRSTACK` *目录栈*

已禁用命令，在 *受限 shell* 中

do 关键字，开始执行 循环 内的命令

done 关键字，终止循环

*DOS* 批处理文件，转换为 shell 脚本

*DOS* 命令，UNIX 的等价命令（**表格**）

*点文件*，"隐藏"的设置和配置文件

双重方括号 **[[ ... ]]** 测试 构造

+   和 评估八进制/十六进制常量

双重括号 **(( ... ))** 算术扩展/评估构造

双引号 **" ... "** *弱引用*

+   *双引号* 引用的 *反斜杠* （**\**）字符

双倍空格的文本文件，使用 sed

* * *

**-e** 文件存在 测试

echo

+   通过 *管道* 传递命令

+   `使用 [命令替换 设置变量](internal.html#ECHOCS)`

+   `$/bin/echo`，外部 *echo* 命令

`elif`，*else* 和 if 的缩写

`else`

使用 openssl 加密文件

`esac`，终止 *case* 构造的关键字

`*环境*变量`

`-eq`，*等于* 整数比较 测试

`欧几里得，筛法`，生成素数的算法

`转义字符`，特殊含义

+   在 $' ... ' 字符串扩展中

+   `与 *Unicode* 字符一起使用`

`$/etc/fstab`（文件系统挂载）文件

`$/etc/passwd`（用户账户）文件

`$EUID`，*有效用户 ID*

`eval，组合并 *评估* 表达式（s），带有变量扩展`

+   `效果`，*示例脚本*

+   `强制 *重新评估* 参数`

+   并且 间接引用

+   `使用 *eval* 的风险`

+   `使用 *eval* 将 *数组*元素转换为命令列表`

+   `使用 *eval* 在变量之间选择`

`[在 [[ ... ]] 内对 *八进制/十六进制* 常量进行评估](testconstructs.html#DBLBRAEV)`

`exec`命令，用于 重定向

`练习`

退出和退出状态

+   `退出`命令

+   `退出状态（*退出代码*，命令的 *返回状态*）`

    `**表**，*具有特殊意义的退出代码*

    `异常`

    `越界`

    `*管道*`退出状态

    `由 *函数返回值*指定`

    `*成功*`，**0**

    `$/usr/include/sysexits.h`，系统文件列出 C/C++ 标准退出代码

`Export`，使变量可用于 子进程

+   `将变量传递给内嵌 *awk* 脚本`

`expr`，*表达式*评估器

+   `子字符串提取`

+   `子字符串 *索引*（字符串中的数值位置）`

+   `子字符串匹配`

`扩展 *正则表达式*`

+   `**?**（问号）匹配零/一个字符`

+   `**( ... )** 表达式组`

+   **\{ N \}** "花括号"，*转义*，匹配字符集的数量

+   **+** *字符匹配*

* * *

因数分解，将整数分解为其素数因子

+   应用：生成素数

false，返回 *失败* (1) 退出状态

字段，一组字符，构成数据项

文件 / 归档

文件描述符

+   关闭

    **n<&-** 关闭输入文件描述符 *n*

    **0<&-**, **<&-** 关闭 `stdin`

    **n>&-** 关闭输出文件描述符 *n*

    **1>&-**, **>&-** 关闭 `stdout`

+   C 中的文件句柄，与

文件加密

find

+   **{}** 花括号

+   **\;** *转义* 分号

过滤器

+   将文件处理实用程序与 - 作为过滤器使用

+   将过滤器输出反馈到 *相同* 过滤器

浮点数，Bash 不识别

fold，一个用于包装文本行的过滤器

创建 *子* 进程

*for* 循环

函数

+   按位置传递的参数 指代

+   使用 [echo 捕获函数的返回值](complexfunct.html#CAPTURERETVAL)

+   *冒号* 作为函数名

+   定义必须先于 函数第一次调用

+   退出状态

+   局部变量

    和 递归

+   将 *数组* 传递给函数

+   传递指针 到一个函数

+   位置参数

+   递归

+   重定向函数的 `stdin`

+   返回

    函数的多个 *返回值*，示例脚本

    从函数返回 *数组*

    *返回* 范围限制，解决方案

+   *Shift* 参数传递 到一个函数

+   不寻常的函数名

* * *

游戏 和 娱乐

+   字母表重排

+   字母表重排，再次

+   宾果数字生成器

+   填字游戏求解器

+   Crypto-Quotes

+   发牌

+   十五拼图

+   赛马

+   骑士之旅

+   "生命"游戏

+   幻方

+   音乐播放脚本

+   Nim

+   弹球机

+   Perquackey

+   玫瑰周围的 petals

+   播客

+   诗歌

+   语音生成

+   汉诺塔

    图形版本

    备用图形版本

getopt，*外部* 命令用于解析脚本的 *命令行* 参数

+   在脚本中模拟

getopts，Bash *内建命令* 用于解析脚本的 *命令行* 参数

+   `$OPTIND` / `$OPTARG`

全局 变量

通配符，文件名扩展

+   正确处理文件名

+   *通配符*

+   不会匹配 `dot files`

黄金比例 (*Phi*)

-ge ，*大于或等于* 整数比较 测试

-gt ，*大于* 整数比较 测试

*groff*，文本标记和格式化语言

Gronsfeld 密码

`$GROUPS`，*组* 用户所属

gzip，压缩工具

* * *

哈希，在表中创建查找键

+   *示例脚本*

head，*echo* 将文本文件开头的行输出到 `stdout`

帮助，提供 Bash 内建命令 的用法摘要

*Here* 文档

+   *匿名* here 文档，使用 **:** 

    注释代码块

    自文档化 脚本

+   *bc* 在 *here 文档* 中

+   *cat* 脚本

+   命令替换

+   *ex* 脚本

+   *函数*，提供输入

+   *Here* 字符串

    计算 黄金比例

    前置文本

    作为 *循环* 的 `stdin`

    使用 *read*

+   *Limit* 字符串

    ! 作为 *限制字符串*

    关闭 *限制字符串* 可能不会缩进

    Dash 选项 限制字符串，`<<-LimitString`

+   文本输出，用于生成程序代码

+   参数替换

    禁用 *参数替换*

+   传递参数

+   临时文件

+   非交互式使用 *vi*

历史命令

`$HOME`，*用户的主目录*

作业分配求解器

`$HOSTNAME`，系统*主机名*

* * *

`$Id`参数，在*rcs*（版本控制系统）

[if [ condition ]; then ...](tests.html#IFTHEN) *测试*结构

+   [if [ condition ]; then ...](tests.html#IFTHEN) *测试*结构

    Fixup 用于 *if-grep* 测试

`$IFS`，*内部字段分隔符*变量

+   默认为*空白字符*

整数比较运算符

in，*关键字*在*for*循环中的 `[list]` 前面

初始化表，`/etc/inittab`

内联组，即代码块

交互式脚本，测试

I/O 重定向

变量的间接引用

+   新符号，在 Bash 的版本 2 中引入（示例脚本）

iptables，数据包过滤和防火墙实用程序

+   用法示例

+   示例脚本

迭代

* * *

作业 ID，表

jot，输出一系列整数。等同于 seq。

+   随机序列生成

又一位 Bash 黑客！

* * *

关键词

+   错误，如果缺失

kill，通过进程 ID 终止一个进程

+   选项 (`-l`, `-9`)

killall，通过*名称*终止一个进程

*killall 脚本* 在 `/etc/rc.d/init.d`

* * *

lastpipe shell 选项

-le ，*小于等于* 整数比较 测试

let，在变量上设置和执行算术运算

+   *C 样式* 递增和递减运算符

限制字符串，在 here 文档中

`$LINENO`，表示变量在脚本中出现的*行号*的变量

链接，文件（使用 *ln* 命令）

+   使用多个名称调用脚本，使用 *ln*

+   *符号* 链接，*ln -s*

列表构造

+   *And* list

+   *Or* list

局部变量

+   和 递归

本地化

逻辑运算符 (`&&`, `||`, 等.)

登出文件，`~/.bash_logout` 文件

环回设备，在块设备上挂载文件

循环

+   break 循环控制命令

+   continue 循环控制命令

+   双括号内的 *C* 样式循环

    *for* 循环

    *while* 循环

+   do (关键字)，开始执行循环内的命令

+   done (关键字)，终止循环

+   *for* 循环

    `*for*` `arg` `*in*` `[list]`; `*do*`

    [*命令替换* 生成 `[list]`](loops1.html#LOOPCS)

    `[list]` 中的 文件名扩展

    [每个 `[list]` 元素中的多个参数](loops1.html#MULTPARAML)

    [省略 `[list]`](loops1.html#OMITLIST)，默认为 位置参数

    [参数化 `[list]`](loops1.html#PARAMLI)

    重定向

+   in，(关键字) 在 *for* 循环中位于 [list] 前面

+   嵌套循环

+   在后台运行循环 *，*脚本示例*

+   当 *do* 在循环的第一行时，需要分号

    *for* 循环

    *while* 循环

+   until 循环

    `*until [条件为真]; do*`

+   while 循环

    `*while [条件]; do*`

    测试括号内的函数调用

    多个条件

    省略 *测试括号*

    重定向

    [*while read*] 构造

+   使用哪种类型的循环

Loopback devices

+   在 `/dev` 目录中

+   挂载 ISO 映像

-lt ，*小于* 整数比较 测试

* * *

m4，宏处理语言

`$MACHTYPE`，*机器类型*

魔数，文件开头的文件类型标记

`Makefile`，包含 make 命令使用的依赖项列表的文件

man，*手册页* (查找)

+   *手册页* 编辑器 (脚本)

mapfile 内置命令，使用文本文件加载数组

数学命令

元意义

摩尔斯电码训练 脚本

取模，算术*余数*运算符

+   应用：生成素数

按揭计算，*示例脚本*

* * *

**-n** 字符串非*空* 测试

命名管道，一个临时的 FIFO 缓冲区

+   *示例脚本*

nc，*netcat*，一个用于 TCP 和 UDP 端口的网络工具包

-ne，*不等于* 整数比较 测试

否定运算符，**!**，反转测试的意义

netstat，网络统计

网络编程

nl，一个用于对文本行进行编号的过滤器

*不覆盖*，Bash 的`-C`选项防止文件被覆盖

*非* 逻辑运算符，**!**

*空* 变量赋值，避免

* * *

**-o** 逻辑或 复合比较测试

伪装

+   *冒号* 作为函数名

+   作业分配

+   又一位 Bash 黑客！

八进制，基数为 8 的数字

od，*八进制转储*

`$OLDPWD` 上一个工作目录

openssl 加密工具

操作符

+   定义

+   优先级

选项，通过命令行或 set 命令传递给 shell 或脚本

*或者* 列表

*或者* 逻辑运算符，**||**

* * *

参数替换

+   *${parameter+alt_value}*

    *${parameter:+alt_value}*

    参数的备选值，如果已设置

+   *${parameter-default}*

    *${parameter:-default}*

    *${parameter=default}*

    *${parameter:=default}*

    默认参数

+   *${!varprefix*}*

    *${!varprefix@}*

    参数*名称*匹配

+   *${parameter?err_msg}*

    参数未设置消息

+   *${parameter}*

    参数*值*

+   *大小写修改* (Bash 版本 4+)。

+   *脚本示例*

+   **表格** 的 *参数替换*

父/子进程问题，*子*进程不能导出变量到父进程

括号

+   命令组

+   括号组的*扩展正则表达式*

+   双括号，在算术扩展中

`$PATH`，系统二进制的*路径*（位置）

+   使用`+=`运算符将目录添加到`$PATH` Appending directories to `$PATH`

路径名，包含给定文件完整*路径*的`filename`

+   解析*路径名*

Perl，编程语言

+   在同一文件中与*Bash*脚本 Combined

+   在*Bash*脚本中嵌入

*Perquackey*-类型的字母重组游戏 (*Quackey*脚本)

*玫瑰周围的 petals*

PID，*进程 ID*，分配给运行进程的识别号

Pipe，**|**，将命令的输出传递给另一个命令或到 shell 的设备

+   在*管道*中避免不必要的命令(Avoiding unnecessary commands)

+   *注释*嵌入其中

+   管道的退出状态

+   Pipefail，设置*set -o pipefail*选项以指示*管道*内的退出状态

+   `$PIPESTATUS`，最后执行的管道的*退出状态*

+   将命令的输出管道到脚本

+   重定向`stdin`，而不是在*管道*中使用 cat

陷阱

+   **-** (短横线)不是重定向操作符

+   **//** (双正斜杠)，cd 命令的行为

+   #!/bin/sh 脚本标题禁用 extended *Bash* features

+   滥用*cat*

+   *CGI*编程，使用脚本

+   在*这里文档*中关闭*限制字符串*，缩进

+   DOS 类型的换行符(\r\n)会导致脚本崩溃

+   *双引号*转义字符(*\*)

+   eval，使用风险

+   脚本内的命令缺少执行权限

+   *退出状态*，异常

+   *退出状态* 算术表达式的*不*等于*错误代码*

+   *导出*问题，*子*进程到*父*进程

+   Extended *Bash* features 不可用

+   在 *test* 括号内未能 *quote* 变量

+   *GNU* 命令集，在跨平台脚本中

+   *let* 的误用：尝试设置字符串变量

+   函数中的多个 *echo* 语句，该函数的输出被捕获的函数

+   *null* 变量赋值

+   数值和字符串比较操作符 *不* 等价

    **=** 和 **-eq** *不* 可互换

+   省略终端 *分号*，在 *花括号* 代码块中

+   管道

    *echo* 到循环

    *echo* 到 *read*（然而，这个问题可以规避）

    *tail* `-f` 到 *grep*

+   在变量中保留 *空白字符*，意外的后果

+   脚本内的 *suid* 命令

+   未记录的 *Bash* 功能，危险

+   *Bash* 的更新破坏了旧脚本

+   未初始化的变量

+   变量名，不合适

+   在 *subshell* 中的变量，作用域有限

+   在 *while-read* 循环中的子 shell

+   空白字符，误用

指针

+   和文件描述符

+   和函数

+   和 *间接引用*

+   和 *变量*

shell 脚本中的兼容性问题

+   设置 *path* 和 *umask*

+   一个 *测试套件* 脚本（Bash 与经典 Bourne shell）

+   使用 *whatis*

位置参数

+   `$@`，作为 *单独* 的词

+   `$*`，作为一个 *单个* 词

+   在函数中

POSIX，*可移植操作系统接口/UNIX*

+   `--posix` 选项

+   1003.2 标准

+   字符类

`$PPID`，父进程的 *进程 ID*

优先级，操作符

[*Prepending*] 文件开头的行，*脚本示例*

质数

+   使用 *factor* 命令生成质数

+   使用 *modulo* 操作符生成质数

+   埃拉托斯特尼筛法，示例脚本

printf，格式化打印命令

`/proc` 目录

+   运行进程，描述文件的

+   在`/proc`中写入文件，*警告*

进程

+   子进程

+   父进程

+   进程 ID（PID）

进程替换

+   比较目录内容

+   为命令提供`stdin`

+   模板

+   *while-read*循环不带*子 shell*

可编程补全（tab 扩展）

提示符

+   `$PS1`，*主提示符*，在命令行中可见

+   `$PS2`，二级提示符

伪代码，作为问题解决方法

`$PWD`，当前工作目录

* * *

Quackey，一种*Perquackey*类型的字母重组游戏（脚本）

问号，**?**

+   在扩展*正则表达式*中的字符匹配

+   单字符*通配符*，在文件名匹配中

+   在一个*C*-风格的三进制（ternary）运算符

引用

+   字符字符串

+   变量

    在*test*括号内

+   *空白字符*，使用*引用*来保留

* * *

随机数

+   `/dev/urandom`

+   `rand()`，awk 中的随机函数

+   `$RANDOM`，Bash 函数，返回一个伪随机整数

+   使用[date 命令生成随机序列](timedate.html#DATERANDREF)

+   使用[jot 生成随机序列](extmisc.html#JOTRANDOM)

+   生成随机字符串

树莓派（单板计算机）

+   准备可启动 SD 卡的脚本

rcs

read，从`stdin`设置变量的值

+   检测*箭头*键

+   选项

+   将*cat*的输出管道连接到*read*

+   “添加”文本

+   将*echo*的输出管道连接到*read*的问题

+   从文件重定向到*read*

+   `$REPLY`，默认*read*变量

+   定时输入

+   *while read* 构造

readline 库

递归

+   演示

+   阶乘

+   斐波那契数列

+   局部变量

+   脚本递归调用自身

+   汉诺塔

重定向

+   代码块

+   执行 `<filename>`,

    重新分配文件描述符

+   I/O 重定向的入门级解释

+   打开文件进行*读取和写入*

    `<>filename`

+   *读取*重定向输入来自文件

+   命令的`stderr`重定向到`stdout`

    `2>&1`

+   使用**-**的`stdin` / `stdout`

+   函数的`stdin`

+   命令的`stdout`重定向到文件

    `>>>` ... `*>>*`

+   命令的`stdout`重定向到*文件描述符* *j*

    `>&j`

+   文件描述符`i`重定向到*文件描述符* *j*

    `i>&j`

+   命令的`stdout`重定向到`stderr`

    `>&2`

+   命令的`stdout` *和* `stderr`重定向到文件

    `&>`

+   tee，在管道管道符过程中将命令的部分输出重定向到文件

参考卡

+   杂项构造

+   参数替换/扩展

+   特殊 shell 变量

+   字符串操作

+   测试运算符

    二进制比较

    文件

*正则表达式*

+   **^** ( caret) 行首

+   **$** (美元符号) *锚点*

+   **.** (点) 匹配单个字符

+   ***** (星号) 任意数量的字符

+   **[ ]** (方括号) 包含要匹配的字符集

+   **\** (反斜杠) 转义，字面地解释后续字符

+   **\< ... \>** (尖括号，*转义*) 单词边界

+   扩展 REs

    **+** *字符匹配*

    **\{ \}** 转义“花括号”

    **[: :]** POSIX 字符类

与[read 命令关联的默认值](internalvariables.html#REPLYREF) `$REPLY`

受限 shell，禁用某些命令的 shell（或脚本）

返回，终止函数的命令

run-parts

+   按顺序运行脚本，无需用户干预

* * *

变量的作用域，定义

脚本选项，在命令行中设置

脚本编写例程，有用的定义和函数的库

二级提示，**`$PS2`**

安全问题

+   nmap，*网络映射器* / 端口扫描器

+   sudo

+   *脚本内的 suid 命令*

+   脚本中的病毒、特洛伊木马和蠕虫

+   编写安全的脚本

sed，基于模式的编程语言

+   **表格**，基本运算符

+   **表格**，运算符的示例

select，用于构建菜单的构造

+   ``**in `*list*`**``省略

信号量

分号必须，当 do *关键字*位于循环的第一行时

+   当终止*花括号*代码块时

seq，输出一系列整数。等同于 jot。

set，更改内部脚本变量的值

+   set -u，如果尝试使用一个*未声明的*变量，则使用错误信息中止脚本。

Shell 脚本，定义

Shell 包装器，嵌入命令或实用程序的脚本

shift，重新分配*位置参数*

`$SHLVL`，*shell 级别*，shell（或脚本）嵌套的深度

shopt，更改*shell 选项*

信号，发送给进程的消息

模拟

+   布朗运动

+   高尔顿板

+   赛马

+   *生命*，游戏

+   PI，通过发射炮弹来近似

+   下推*栈*

单引号 (**' ... '**) *强* 引用

套接字，与 I/O 端口关联的通信节点

排序

+   冒泡排序

+   插入排序

source，执行脚本或在脚本内部导入文件

+   传递位置参数

*垃圾邮件*，处理

+   *示例脚本*

+   *示例脚本*

+   *示例脚本*

+   *示例脚本*

特殊字符

栈

+   定义

+   模拟一个*下推栈*，示例脚本

标准差，示例脚本

启动文件, Bash

`stdin` 和 `stdout`

计时器，示例脚本

字符串

+   **=~** 字符串匹配运算符

+   比较

+   长度

    `*${#string}*`

+   操作

+   操作，使用 awk

+   *空* 字符串，测试

+   保护字符串 以防止展开和/或重新解释，*脚本示例*

    *取消保护* 字符串，*脚本示例*

+   *strchr()*，等价于

+   *strlen()*，等价于

+   strings 命令，在二进制或数据文件中查找可打印的字符串

+   子串提取

    ${string:position}

    ${string:position:length}

    使用 *expr*

+   子串 *索引*（字符串中的数值位置）

+   子串 *匹配*，使用 expr

+   子串 *删除*

    ${var#Pattern}

    ${var##Pattern}

    ${var%Pattern}

    ${var%%Pattern}

+   子串替换

    ${string/substring/replacement}

    ${string//substring/replacement}

    ${string/#substring/replacement}

    ${string/%substring/replacement}

    *脚本示例*

+   **表格** 中 *字符串/子串* 操作和提取运算符

*强引用* **' ... '**

脚本编写样式表

子 shell

+   括号内的命令列表

+   变量，`$BASH_SUBSHELL` 和 `$SHLVL`

+   子 shell 中的变量

    *作用域* 限制，但 ...

    ... 可以在子 shell 外部访问吗？

su *替换用户*，以不同的用户或作为 *root* 登录

suid (*设置用户 ID*) 文件标志

+   *suid* 命令在脚本中，不建议使用

符号链接

交换文件

* * *

Tab completion

表查找，脚本示例

tail，*echo* 将文本文件的（尾部）行输出到 `stdout`

tar，归档工具

tee，在管道中途将命令的部分输出重定向到文件

终端

+   setserial

+   setterm

+   stty

+   tput

+   墙

*测试*命令

+   Bash *内建*

+   外部命令，`/usr/bin/test`（相当于`/usr/bin/`）

[测试构造

测试操作符

+   **-a** 逻辑与复合比较

+   **-e** 文件存在

+   **-eq** 等于（整数比较）

+   **-f** 文件是**常规**文件

+   **-ge** 大于等于（整数比较）

+   **-gt** 大于（整数比较）

+   **-le** 小于等于（整数比较）

+   **-lt** 小于（整数比较）

+   **-n** 非零长度（字符串比较）

+   **-ne** 不等于（整数比较）

+   **-o** 逻辑或复合比较

+   **-u** 设置*suid*标志，文件测试

+   **-z** 长度为零（字符串比较）

+   **=** 等于（字符串比较）

    **==** 等于（字符串比较）

+   **<** 小于（字符串比较）

+   **<** 小于，（整数比较，在双括号内）

+   **<=** 小于等于，（整数比较，在**双括号**内）

+   **>** 大于（字符串比较）

+   **>** 大于，（整数比较，在**双括号**内）

+   **>=** 大于等于，（整数比较，在**双括号**内）

+   **||** 逻辑或

+   **&&** 逻辑与

+   **!** 否定操作符，反转测试的退出状态

    **!=** 不等于（字符串比较）

+   **测试操作符**的**表格**

    二进制比较

    文件

文本和文本文件处理

时间/日期

定时输入

+   使用*read -t*

+   使用*stty*

+   使用定时循环

+   使用`$TMOUT`

Bash 脚本**技巧和提示**

+   数组，作为函数的**返回值**（assortedtips.html#RETARRAY）

    *关联* 数组比数字索引数组更高效(优化)

+   使用 *echo* 捕获函数的返回值

+   *CGI* 编程，使用脚本

+   注释块

    使用 *匿名文档*

    使用 *if-then* 构造

+   注释标题，特殊用途

+   *C* 样式语法，用于操作变量

+   双倍间距文本文件

+   以短横线为前缀的文件名，移除

+   过滤器，将输出反馈到*相同*的过滤器

+   函数 *返回* 值的解决方案

+   *if-grep* 测试修复

+   库中包含有用的定义和 *函数*

+   *null* 变量赋值，避免

+   将 *数组* 传递给函数

+   `$PATH`，使用 `+=` 运算符追加

+   *预置* 文件开头的行

+   进度条 模板

+   伪代码

+   rcs

+   将 *测试* 重定向到 `/dev/null` 以抑制输出

+   按顺序运行脚本而不需要用户干预，使用 run-parts

+   脚本作为嵌入命令

+   脚本 *可移植性*

    设置 *路径* 和 *umask*

    使用 *whatis*

+   设置脚本变量到嵌入的 *sed* 或 *awk* 代码块

+   通过禁用 *unicode*来加快脚本执行速度

+   子 shell 变量，在子 shell 外部访问

+   测试一个变量以查看它是否只包含数字

+   测试命令是否存在，使用 type

+   跟踪脚本使用情况

+   *while-read* 循环没有 *子 shell*

+   小部件，从脚本中调用

`$TMOUT`，超时间隔

令牌，一个可能扩展为关键字或命令的符号

tput，终端控制命令

tr，字符转换过滤器

+   DOS 到 Unix 文本文件转换

+   选项

+   Soundex，*示例脚本*

+   变体

*陷阱*，指定在接收到信号时执行的操作

*三元（三元）* 操作符，*C* 风格，`**var>10?88:99**`

+   在 *双括号* 构造中

+   在 *let* 构造中

true，返回 *成功*（0）退出状态

typeset 内置命令

+   选项

* * *

`$UID`，用户 ID 号

unalias，用于删除 别名

uname，输出系统信息

Unicode，表示字母和符号的编码标准

+   禁用 *unicode* 以优化脚本

未初始化的变量

uniq，从排序文件中过滤重复行的过滤器

unset，删除 shell 变量

until 循环

*until [条件为真]; do*

* * *

*变量*

+   数组操作

+   赋值

    *脚本示例*

    *脚本示例*

    *脚本示例*

+   *Bash* 内部变量

+   设置 *sed* 或 *awk* 代码块，将变量设置为

+   *C-style* 递增/递减/三元操作

+   使用 *set* 修改内部脚本变量的值

+   declare，用于修改变量的属性

+   使用 *unset* 删除 shell 变量

+   环境

+   扩展 / 子串替换 操作符

+   间接引用

    `*eval variable1=\$$variable2*`

    新符号

    `*${!variable}*`

+   整数

+   整数 / 字符串（变量是无类型的）

+   长度

    `*${#var}*`

+   Lvalue

+   操作和扩展

+   *变量* 的 *名称* 和 *值*，区分

+   *Null* 字符串，测试

+   *Null* 变量赋值，避免

+   引号

    在 *测试* 括号内

    保留 *空白*

+   rvalue

+   设置为 *null* 值

+   在 *子 shell* 中对父 shell 不可见

+   测试变量 是否只包含数字

+   Typing，限制变量的属性

+   未声明，错误信息

+   未初始化

+   未引用的变量，*分割*

+   Unsetting

+   未类型化

* * *

wait，挂起脚本执行

+   修复脚本挂起

[*弱引用*] **" ... "**

while 循环

*while [ condition ]; do*

+   C 样式语法

+   在*测试*括号内调用*函数*

+   多个条件

+   省略 *测试* 括号

+   [*while read*] 构造

    避免 *子 shell*

空白符，空格、制表符和换行符

+   `$IFS` 默认为

+   不恰当的使用

+   here 文档中前一个 *结束字符串* 的错误

+   前一个脚本注释

+   *引用*，以保留字符串或变量中的 *空白符*

+   [[:space:]](x17129.html#WSPOSIX)，*POSIX* 字符类

who，有关已登录用户的信息

+   w

+   whoami

+   logname

小部件

通配符 字符

+   星号 *

+   在[`*[列表]*`构造](loops1.html#LIGLOB)

+   问号 ?

+   不会匹配 `点文件`

单词分割

+   定义

+   由 *命令替换* 导致

包装器，shell

* * *

xargs，分组参数的过滤器

+   花括号

+   限制传递的参数

+   选项

+   逐个处理参数

+   空白符，处理

* * *

yes

+   模拟

* * *

**-z** 字符串是 *空*

*僵尸*，一个已终止但尚未被其父进程 杀死的进程
